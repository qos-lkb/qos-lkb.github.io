---
description: HKDSE 科學模擬平台核心脈絡與文件索引
alwaysApply: true
---

# Science Simulations — 核心脈絡

本儲存庫是 **HKDSE／中學科學** 互動模擬與學習平台：獨立 **HTML 模擬**（可靜態託管）+ **PHP 8+／MariaDB**（SPA、`/api/v1`、後台）。

## 技術取向

- **HTML5 + Vanilla JS**；樣式以 **Tailwind CSS（CDN）** 為主；部分模擬用 Three.js、Chart.js、MathJax、少數用 React + Babel standalone。
- 動態功能經 **REST API**；目錄權威為 **MariaDB**（非 CSV）。
- 變更應與同目錄、同類型檔案的既有風格一致。詳見 **`rule.md`**、**`architecture.md`**、**`README.md`**。

## 目錄約定

- `physics/`、`chem/`、`biology/`、`science/`、`astronomy/`、`other/` 等 — 模擬 HTML（磁碟為 HTML source of truth）
- `app/` — 使用者 SPA；`api/v1/` — REST；`admin/`／`portal/` — 後台（將收斂為 API-only UI）
- `scripts/apply_schema.php`、`scripts/sync_simulations_to_db.php` — 升級與同步

## 助理行為

- 僅修改與任務直接相關的檔案；新增模擬遵守 `rule.md` 命名與結構。
- 使用者偏好以 **繁體中文** 說明（程式註解依既有檔案語言）。
