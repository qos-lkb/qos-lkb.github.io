---
description: 模擬實驗 HTML 檔的命名、結構與樣式慣例
globs: "**/*.html"
alwaysApply: false
---

# 模擬 HTML 規則

詳盡條文見根目錄 **`rule.md`**。此處為助理必守摘要。

## 檔名

- 小寫、`snake_case`；物理主線建議 `0X0Y_description.html`；選修路徑加前綴 `e`（如 `e02/e0201_….html`）。
- 避免空白與特殊字元（`-`、`_` 可接受）。

## 文件骨架

- `<!DOCTYPE html>`、`lang`（`zh-Hant` 或 `en`）、`charset=UTF-8`、`viewport`、有意義的 `<title>`。
- 優先語意標籤；Tailwind 與同資料夾檔案一致。
- 列表收錄：在 **MariaDB** 建 metadata，並以 `scripts/sync_simulations_to_db.php` 同步 HTML；SPA 讀 **`/api/v1/simulations/{slug}/html`**。

## 腳本與依賴

- 沿用鄰近模擬的 CDN 版本；不要無故升級 major 或混用衝突的 Three/React。
- 模擬應在無建置步驟下以雙擊或靜態伺服器即可運作（除非明確依賴 PHP）。

## 資產

- 截圖放該科別慣用目錄（如 `physics/screenshots/`），路徑寫入 DB `screenshot_path`。
