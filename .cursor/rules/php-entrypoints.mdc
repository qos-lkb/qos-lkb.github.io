---
description: PHP 入口與 API／lib 編修慣例
globs: "**/*.php"
alwaysApply: false
---

# PHP 規則

## 範圍

- 業務邏輯：`includes/*_lib.php`（逐步遷入 `src/` PSR-4）
- REST：`api/v1/router.php` + `api/v1/handlers/`
- 後台／portal 頁（過渡期）：`admin/`、`portal/` — 目標改為只打 API
- 入口薄殼：`app/index.php`、`login.php`、`index.php`（302 → `app/`）

## 慣例

- 新檔與重大改動使用 `declare(strict_types=1);`
- 變更請求：Session `SCI_SIM_SESSID` + `X-CSRF-Token`（或 form `csrf`）
- 避免在輸出已開始後 `header()`；上傳沿用既有驗證與路徑邏輯
- Schema：改 `schema.sql` + `schema_upgrade_all.sql`，既有庫用 `php scripts/apply_schema.php`，並更新 `change_log.md`

## Legacy（勿擴充）

- `simulation_view.php`、`index_csv_editor.php`：僅轉址；新功能走 `/api/v1`
- 勿恢復 CSV 目錄或 `parseCSV` 驅動列表
