---
description: 目錄與模擬收錄改走 MariaDB／API（index.csv 已停用）
globs: schema.sql,admin/simulations.php,api/v1/handlers/simulations.php,scripts/sync_simulations_to_db.php
alwaysApply: false
---

# 模擬目錄（非 CSV）

**`index.csv` 已停用。** 目錄權威來源為 **MariaDB**（`subjects`／`topics`／`simulations`），前台經 **`GET /api/v1/catalog`** 與 SPA 讀取。

## 新增／更新模擬

1. 在學科目錄新增靜態 **`.html`**（磁碟為 HTML **source of truth**）。
2. 於後台或 API 建立／更新 metadata（slug、標題、科目、截圖、status）。
3. 將 HTML 同步進 DB：`php scripts/sync_simulations_to_db.php`（可加 `--dry-run`／`--create-missing`）。
4. 發布後 SPA iframe 載入 **`/api/v1/simulations/{slug}/html`**（勿再依賴 `simulation_view.php`）。

## 部署注意

- **學校 PHP 主機**：DB + API + `app/`。
- **GitHub Pages**：僅靜態 HTML；與 DB 內容需靠 Git／sync 流程對齊。
